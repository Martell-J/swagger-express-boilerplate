"use strict";

/*

  Contains relations for models which have their target foreignKeys on the
  target table from the source table

  // Ordered by Source Model Alphebetically Descending

*/

module.exports = (models) =>
  new Promise((resolve) => {



    return resolve(models);

  });
