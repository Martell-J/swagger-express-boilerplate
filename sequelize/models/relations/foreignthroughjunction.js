"use strict";

/*

  Contains relations for models which have their target foreignKeys on junction
  tables between the source and target model.

*/

module.exports = (models) =>
  new Promise((resolve) => {



    return resolve(models);

  });
